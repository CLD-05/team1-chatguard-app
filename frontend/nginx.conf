server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # 정적 서빙 + React Router fallback만.
    # /api·/ws는 ALB Ingress가 chat-server로 직결한다(config 레포 D47) — nginx 프록시 불필요.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
