server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router 대응 — 모든 경로를 index.html로
    location / {
        try_files $uri $uri/ /index.html;
    }
}
