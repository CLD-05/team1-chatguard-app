server:
  shutdown: graceful

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s
  config:
    import: optional:file:./.env[.properties]
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://localhost:3306/chatguard_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&useUnicode=true}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jackson:
    property-naming-strategy: SNAKE_CASE
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # Redis
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      # E5: Lettuce commandTimeout — Redis 장애 시 health redis contributor 응답을 3s 내로 상한
      timeout: 3s

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}

logging:
  level:
    com.chatguard: DEBUG
    org.springframework.web.socket: DEBUG

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  endpoint:
    health:
      show-details: always
      # E5: /actuator/health/liveness·/readiness 노출 고정 (K8s 플랫폼 자동감지 비의존)
      # liveness group=livenessState만(Redis 무관) — liveness=프로세스 생존, readiness=의존성
      probes:
        enabled: true
  # 리뷰 반영 [권장 해결]: p95 관측을 위한 Histogram 노출 설정
  metrics:
    distribution:
      percentiles-histogram:
        chat_broadcast_latency_seconds: true


